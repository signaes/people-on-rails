<% if person_is_logged_in? %>
  <%= link_to t('person.profile.link'), profile_path %>
  <%= link_to t('logout.link'), logout_path, method: :delete %>
<% else %>
  <%= link_to t('login.link'), login_path %>
  <%= link_to t('signup.link'), signup_path %>
<% end %>
